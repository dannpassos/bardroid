<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.danilo.projetoandroid" >

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_bardroid"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".TelaDeAberturaActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CadastrarProdutoActivity"
            android:label="@string/title_activity_cadastrar_produto" >
        </activity>
        <activity
            android:name=".ListaProdutosActivity"
            android:label="@string/title_activity_lista_produtos" >
        </activity>
        <activity
            android:name=".EditarProdutoActivity"
            android:label="@string/title_activity_editar_produto" >
        </activity>
        <activity
            android:name=".SobreActivity"
            android:label="@string/title_activity_sobre" >
        </activity>
        <activity
            android:name=".CadastrarPedidoActivity"
            android:label="@string/title_activity_cadastrar_pedido" >
        </activity>
        <activity
            android:name=".FecharContaActivity"
            android:label="@string/title_activity_fechar_conta" >
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".LudosMapsActivity"
            android:label="@string/title_activity_ludos_maps" >
        </activity>
        <activity
            android:name=".TelaPrincipalActivity"
            android:label="TelaPrincipalActivity" >
        </activity>
    </application>

</manifest>
